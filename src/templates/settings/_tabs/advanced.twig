{% import '_includes/forms.twig' as forms %}
{% from 'co-pilot/settings/_macros' import configWarning %}

    <h3>Agent</h3>

    {{ forms.textField({
        label: 'Max Agent Iterations',
        instructions: 'Maximum number of tool call iterations per request.',
        id: 'maxAgentIterations',
        name: 'maxAgentIterations',
        value: settings.maxAgentIterations,
        type: 'number',
        min: 1,
        max: 50,
        disabled: config.maxAgentIterations is defined,
        warning: config.maxAgentIterations is defined ? configWarning('maxAgentIterations'),
        errors: settings.getErrors('maxAgentIterations'),
    }) }}

    {{ forms.textField({
        label: 'Max Context Tokens',
        instructions: 'Maximum token budget for entry context sent to the AI.',
        id: 'maxContextTokens',
        name: 'maxContextTokens',
        value: settings.maxContextTokens,
        type: 'number',
        min: 1000,
        max: 128000,
        step: 1000,
        disabled: config.maxContextTokens is defined,
        warning: config.maxContextTokens is defined ? configWarning('maxContextTokens'),
        errors: settings.getErrors('maxContextTokens'),
    }) }}

    {{ forms.textField({
        label: 'Default Search Limit',
        instructions: 'Default number of results returned by search.',
        id: 'defaultSearchLimit',
        name: 'defaultSearchLimit',
        value: settings.defaultSearchLimit,
        type: 'number',
        min: 1,
        max: 100,
        disabled: config.defaultSearchLimit is defined,
        warning: config.defaultSearchLimit is defined ? configWarning('defaultSearchLimit'),
        errors: settings.getErrors('defaultSearchLimit'),
    }) }}

    <hr>

    <h3>Serialization</h3>

    {{ forms.textField({
        label: 'Default Serialization Depth',
        instructions: 'Default depth for nested entry serialization.',
        id: 'defaultSerializationDepth',
        name: 'defaultSerializationDepth',
        value: settings.defaultSerializationDepth,
        type: 'number',
        min: 1,
        max: 10,
        disabled: config.defaultSerializationDepth is defined,
        warning: config.defaultSerializationDepth is defined ? configWarning('defaultSerializationDepth'),
        errors: settings.getErrors('defaultSerializationDepth'),
    }) }}

    {{ forms.textField({
        label: 'Max Serialization Depth',
        instructions: 'Maximum allowed serialization depth.',
        id: 'maxSerializationDepth',
        name: 'maxSerializationDepth',
        value: settings.maxSerializationDepth,
        type: 'number',
        min: 1,
        max: 10,
        disabled: config.maxSerializationDepth is defined,
        warning: config.maxSerializationDepth is defined ? configWarning('maxSerializationDepth'),
        errors: settings.getErrors('maxSerializationDepth'),
    }) }}

    <hr>

    <h3>Appearance</h3>

    {{ forms.textField({
        label: 'Plugin Name',
        instructions: 'Override the name shown in the control panel sidebar.',
        id: 'pluginName',
        name: 'pluginName',
        value: settings.pluginName,
        disabled: config.pluginName is defined,
        warning: config.pluginName is defined ? configWarning('pluginName'),
        errors: settings.getErrors('pluginName'),
    }) }}

    <hr>

    <h3>Data Retention</h3>

    {{ forms.textField({
        label: 'Audit Log Retention (Days)',
        instructions: 'Number of days to keep audit log entries. Older entries are automatically deleted during garbage collection. Set to 0 to keep logs indefinitely.',
        id: 'auditLogRetentionDays',
        name: 'auditLogRetentionDays',
        value: settings.auditLogRetentionDays,
        type: 'number',
        min: 0,
        max: 365,
        disabled: config.auditLogRetentionDays is defined,
        warning: config.auditLogRetentionDays is defined ? configWarning('auditLogRetentionDays'),
        errors: settings.getErrors('auditLogRetentionDays'),
    }) }}
